node_modules/
.env
.env.local
.env.production
.env.*.local

# Local gitleaks binary
gitleaks.exe
gitleaks_*.zip

quantum-chat.json
/quantum-chat.json
# Local blob storage (dev fallback when Google Drive is unset)
uploads/
!uploads/.gitkeep

# Auto-generated Web Push VAPID keys (use .env in production)
data/vapid-keys.json
